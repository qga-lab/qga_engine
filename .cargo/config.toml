# Ryzen 9 3900X (Zen 2) + local LLVM codegen.
# Thin LTO lives in the workspace [profile.release].
[build]
rustflags = ["-C", "target-cpu=native"]

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=native"]
