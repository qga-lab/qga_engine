# Parked (not this crate)

Phase 7 is the organizational gate. These stay out of `qga_engine`.

- New repo or third Vulkan front-end.
- Treating Z-map, Magic Islands, six-species iris, or W_g as theorems inside the binary.
- Unifying Kingdom and classical Hopf. Kingdom is `legacy_portal_map` and is not Hopf.
- Changing the 32-byte particle/fibre contract or 256-byte frame uniforms without a coordinated `qga_gpu` rev.
- Publishing crates.io / closing OP1 while Phase 7 is open.
- Tagging `v0.1.0` while `shellscan` still path-depends on the sibling tree
  (`inner_cone` pins `qga_engine@7e7866b` + `qga_gpu@b9c9994`).
- Floating `qga_gpu` on `main`. Pin stays `b9c9994`.

## PIC identities (not cosmos)

Esirkepov, Villasenor–Buneman, Yee **J**, Boris/Vay need faces and ρ.
`nbody.wgsl` has neither (Newtonian all-pairs + midplane spring + optional
species annulus). Wiring a depositor into cosmos would be a different scene
and a different claim grade. Parked here, not in the shader.
